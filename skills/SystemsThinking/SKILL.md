---
name: SystemsThinking
algorithm_capability: thinking
description: Iceberg, causal loops, Meadows leverage points; recurring problems, structural causes
---

# SystemsThinking

Iceberg, causal loops, Meadows leverage points; recurring problems, structural causes

Phases: OBSERVE, THINK.

## Algorithm-capability stub

This SKILL.md is a stub registered with the closed-enum phantom audit
(`src/algorithm/capabilities.ts`). The full workflow is owned by PAI at
`~/.claude/skills/SystemsThinking/`. claude-hooks-ts ships the stub so the audit gate
has substrate to enforce against and so the model can reference the
capability by its canonical name.
