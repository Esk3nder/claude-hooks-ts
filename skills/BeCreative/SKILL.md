---
name: BeCreative
algorithm_capability: thinking
description: Verbalized Sampling divergent ideation; novel approaches needed
---

# BeCreative

Verbalized Sampling divergent ideation; novel approaches needed

Phases: OBSERVE, BUILD.

## Algorithm-capability stub

This SKILL.md is a stub registered with the closed-enum phantom audit
(`src/algorithm/capabilities.ts`). The full workflow is owned by the upstream spec at
`~/.claude/skills/BeCreative/`. claude-hooks-ts ships the stub so the audit gate
has substrate to enforce against and so the model can reference the
capability by its canonical name.
