---
name: Council
algorithm_capability: thinking
description: Multi-agent debate with visible transcripts; multi-perspective decisions, controversial direction
---

# Council

Multi-agent debate with visible transcripts; multi-perspective decisions, controversial direction

Phases: THINK, PLAN.

## Algorithm-capability stub

This SKILL.md is a stub registered with the closed-enum phantom audit
(`src/algorithm/capabilities.ts`). The full workflow is owned by this package at
`~/.claude/skills/Council/`. claude-hooks-ts ships the stub so the audit gate
has substrate to enforce against and so the model can reference the
capability by its canonical name.
