---
name: IterativeDepth
algorithm_capability: thinking
description: Multi-angle exploration; default at Extended+ when budget allows deeper understanding.
---

# IterativeDepth

Use when an important task warrants exploring the full problem space before
ISC writing — when "what was literally said" diverges from "what's actually
being asked," when the scope is multi-faceted, when hidden assumptions might
distort the answer.

Phases: OBSERVE.

## Algorithm-capability stub

This SKILL.md is a stub registered with the closed-enum phantom audit
(`src/algorithm/capabilities.ts`). The full multi-pass workflow is owned by
at `~/.claude/skills/IterativeDepth/`. claude-hooks-ts ships the stub so
the audit gate has substrate to enforce against and so the model can
reference the capability by its canonical name.
